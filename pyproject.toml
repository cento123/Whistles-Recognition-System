[project]
name = "whistles_recognition_system"
version = "1.2.3"
description = "WRS to detect whistles"
authors = [{name="cento123"},{name="ddietor"}]

[tool.setuptools]
packages = ["src"]

[tool.setuptools.package-data]
src = ["*.yaml"]
